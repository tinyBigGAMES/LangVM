enum Colors {
  Red,
  Green,
  Blue
}

routine main() {
  let r: any = Red;
  let g: any = Green;
  let b: any = Blue;
  assert(r != g, "Red != Green");
  assert(g != b, "Green != Blue");
  assert(r != b, "Red != Blue");
}
